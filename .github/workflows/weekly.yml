name: Weekly Report

on:
  workflow_dispatch: {}
  schedule:
    - cron: "0 6 * * 0"

jobs:
  build:
    runs-on: ubuntu-latest
    # DETTE MANGLER HØJST SANDSYNLIGT:
    permissions:
      contents: write
    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install requirements
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          
      - name: Reparer historik (Backfill)
        run: |
          python data/backfill_data.py

      - name: Build Weekly Report
        run: |
          python reporting/build_weekly_report.py
        
      - name: Build weekly HTML
        run: |
          python reporting/build_weekly_report.py

      # DETTE MANGLER OGSÅ:
      - name: Commit and Push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add build/weekly.html
          git commit -m "Ugentlig rapport opdateret [skip ci]" || echo "Ingen ændringer"
          git push
