name: Monthly Report

on:
  workflow_dispatch: {}
  schedule:
    # Kører hver lørdag kl. 07:00 (06:00 UTC)
    - cron: "0 6 * * 6"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install requirements
        run: |
          python -m pip install --upgrade pip
          pip install jinja2 requests

      - name: Build Monthly Report
        run: |
          python reporting/build_monthly_report.py

      # ------------------------------------------------
      # TRAIL STOP NOTIFIKATION
      # Produktionstærskel: 3.0%
      # Skift til 0.1 midlertidigt hvis du vil teste mailen.
      # ------------------------------------------------
      - name: Send Trail Stop Alert (3.0%)
        env:
          MAIL_USERNAME:    ${{ secrets.MAIL_USERNAME }}
          MAIL_PASSWORD:    ${{ secrets.MAIL_PASSWORD }}
          MAIL_RECIPIENTS:  ${{ secrets.MAIL_RECIPIENTS }}
        run: |
          python reporting/send_alert.py \
            --trail-pct 3.0 \
            --report-type "Monthly Deep Dive"

      - name: Commit and Push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add build/monthly.html data/high_water_marks.json
          git commit -m "Monthly Deep Dive + HWM opdateret [skip ci]" || echo "Ingen ændringer"
          # Pull med rebase inden push for at undgå rejected push hvis weekly
          # har committet til main i mellemtiden (de kører begge lørdag morgen).
          git pull --rebase origin main
          git push
