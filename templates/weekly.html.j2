<!doctype html>
<html lang="da">
<head>
  <meta charset="utf-8">
  <title>TrendAgent Weekly</title>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <style>
    body { font-family: -apple-system, sans-serif; background: #f0f2f5; padding: 20px; color: #1a1a1a; }
    .container { max-width: 1050px; margin: auto; background: white; padding: 25px; border-radius: 12px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); }
    .header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 25px; }
    .score-box { border: 2px solid #22c55e; background: #e6f4ea; padding: 15px; border-radius: 8px; text-align: center; min-width: 150px; }
    .grid { display: flex; gap: 20px; margin-bottom: 25px; }
    .box { flex: 1; padding: 15px; border-radius: 8px; border: 1px solid #ddd; background: #fafafa; }
    .chart-container { height: 250px; margin-bottom: 30px; }
    table { width: 100%; border-collapse: collapse; margin-top: 20px; }
    th, td { padding: 12px; border-bottom: 1px solid #eee; text-align: left; font-size: 14px; }
    th { background: #f8f9fa; color: #666; font-size: 12px; text-transform: uppercase; letter-spacing: 0.5px; }
    .active { background: #fffde7; font-weight: bold; }
    .trend-bull { color: #22c55e; font-weight: bold; }
    .trend-bear { color: #ef4444; font-weight: bold; }
    
    /* Footer Styling - Udvidet metodik */
    .footer { margin-top: 40px; padding: 30px; background: #f8f9fa; border-radius: 12px; border-top: 2px solid #eee; }
    .footer h2 { margin-top: 0; color: #2c3e50; border-bottom: 1px solid #ddd; padding-bottom: 10px; font-size: 22px; }
    .footer h3 { color: #333; font-size: 16px; margin-top: 20px; margin-bottom: 10px; }
    .footer-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 40px; margin-top: 20px; }
    .footer-col p { font-size: 13.5px; line-height: 1.6; color: #444; margin-bottom: 12px; }
    .footer-col ul { padding-left: 20px; margin: 0; }
    .footer-col li { font-size: 13px; color: #555; margin-bottom: 8px; }
    .strategy-box { margin-top: 30px; padding: 20px; background: #fff; border-radius: 8px; border-left: 5px solid #22c55e; box-shadow: 0 2px 8px rgba(0,0,0,0.05); }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div>
        <h1>📈 TrendAgent Weekly</h1>
        <small>Uge {{ week_number }} | Dato: {{ report_date }}</small>
      </div>
      <div class="score-box">
        <small>UGENS AFKAST (⭐)</small><br>
        <strong style="font-size: 24px; color: #15803d;">{{ '%.2f'|format(avg_portfolio_return) }}%</strong>
      </div>
    </div>

    {% if portfolio_alerts or market_opportunities %}
    <div style="background: #fffde7; border-left: 5px solid #fbc02d; padding: 20px; margin-bottom: 25px; border-radius: 4px;">
      {% for a in portfolio_alerts %}
        <div style="margin-bottom: 8px; font-size: 16px;">{{ a.msg }} <strong>{{ a.name }}</strong></div>
      {% endfor %}
      
      {% if market_opportunities %}
        <div style="margin-top:10px; padding-top: 10px; border-top: 1px solid rgba(0,0,0,0.05);">
          <strong>🎯 Nye BULL-muligheder:</strong> 
          <span style="color: #444;">
          {% for o in market_opportunities %}{{ o.name }}{{ ", " if not loop.last }}{% endfor %}
          </span>
        </div>
      {% endif %}
    </div>
    {% endif %}

    <div class="grid">
      <div class="box" style="border-top: 4px solid #22c55e;">
        <h4 style="margin-top:0;">🚀 Ugens Top 5</h4>
        {% for t in top_up %}
          <div style="display:flex; justify-content:space-between; font-size:12px; margin-bottom:6px;">
            <span>{{ t.name[:35] }}</span> <strong style="color: #16a34a;">+{{ '%.2f'|format(t.week_change_pct) }}%</strong>
          </div>
        {% endfor %}
      </div>
      <div class="box" style="border-top: 4px solid #ef4444;">
        <h4 style="margin-top:0;">⚠️ Ugens Bund 5</h4>
        {% for t in top_down %}
          <div style="display:flex; justify-content:space-between; font-size:12px; margin-bottom:6px;">
            <span>{{ t.name[:35] }}</span> <strong style="color: #dc2626;">{{ '%.2f'|format(t.week_change_pct) }}%</strong>
          </div>
        {% endfor %}
      </div>
    </div>

    <div class="chart-container">
      <canvas id="momentumChart"></canvas>
    </div>

    <table>
      <thead>
        <tr>
          <th>Fond</th>
          <th>Uge %</th>
          <th>Total %</th>
          <th>Trend</th>
          <th>Momentum</th>
          <th>ÅTD</th>
          <th>Drawdown</th>
        </tr>
      </thead>
      <tbody>
        {% for row in rows %}
        <tr class="{{ 'active' if row.is_active else '' }}">
          <td>{{ '⭐' if row.is_active else '' }} {{ row.name[:45] }}</td>
          
          <td style="color: {{ '#16a34a' if row.week_change_pct > 0 else '#dc2626' }}; font-weight: 500;">
            {{ '%.2f'|format(row.week_change_pct) }}%
          </td>

          <td style="font-weight: bold; color: {{ '#16a34a' if (row.total_return or 0) > 0 and row.week_change_pct >= 0 else '#dc2626' if row.total_return is not none else '#666' }}">
            {{ '%.2f'|format(row.total_return) if row.total_return is not none else '-' }}%
          </td>

          <td class="{{ 'trend-bull' if row.trend_state == 'UP' else 'trend-bear' }}">
            {{ 'BULL' if row.trend_state == 'UP' else 'BEAR' }}
          </td>
          <td>{{ row.momentum }}%</td>
          <td>{{ '%.2f'|format(row.ytd_return) }}%</td>
          <td style="color: #dc2626;">{{ '%.2f'|format(row.drawdown) }}%</td>
        </tr>
        {% endfor %}
      </tbody>
    </table>

    <div class="footer">
      <h2>📖 Fuld Metodik & Definitioner</h2>
      
      <div class="footer-grid">
        <div class="footer-col">
          <h3>📈 Trend-analyse & Alarmer</h3>
          <p>Ugerapporten opsummerer de tekniske bevægelser for at sikre, at vi altid er på den rigtige side af markedet.</p>
          <ul>
            <li><strong>Trend-skifte (Shift Detection):</strong> Systemet sammenligner dagens tilstand med gårsdagens. Hvis en fond krydser MA200-grænsen, genereres en automatisk alarm (🚀 KØB eller ⚠️ SÆLG).</li>
            <li><strong>Nye BULL-muligheder:</strong> Fonde fra overvågningslisten, der netop er brudt op over deres MA200, hvilket indikerer en ny potentiel optrend.</li>
            <li><strong>Trend-status:</strong> <strong>BULL</strong> (Kurs over MA200) indikerer styrke. <strong>BEAR</strong> (Kurs under MA200) indikerer svaghed og øget risiko.</li>
          </ul>
        </div>
        
        <div class="footer-col">
          <h3>📊 Beregninger & Nøgletal</h3>
          <p>Her er de præcise definitioner på de tal, du ser i rapporten:</p>
          <ul>
            <li><strong>Ugens Afkast (⭐):</strong> Det vægtede gennemsnit af alle aktive positioners kursændring i den forgangne uge.</li>
            <li><strong>Total %:</strong> Dit urealiserede afkast siden køb. Markeret med <span style="color: #dc2626;">rød</span>, hvis ugen har været negativ, eller hvis den samlede position er i minus.</li>
            <li><strong>MA200 Momentum:</strong> Den procentvise afstand fra den nuværende kurs til det 200-dages glidende gennemsnit.</li>
            <li><strong>Drawdown (DD):</strong> Det aktuelle fald fra den højeste kurs (ATH) målt i systemets historik.</li>
          </ul>
        </div>
      </div>

      <div class="strategy-box">
        <strong>Vores Systematiske Tilgang:</strong> Ved at kombinere teknisk analyse af de glidende gennemsnit (MA200) med realtidsdata for afkast, fjerner vi mavefornemmelser fra ligningen. Vi følger trenden: Vi køber styrke og sælger svaghed, styret af hårde data og objektive signaler.
      </div>
    </div>
  </div>

  <script>
    const ctx = document.getElementById('momentumChart').getContext('2d');
    new Chart(ctx, {
      type: 'bar',
      data: {
        labels: {{ chart_labels | tojson }},
        datasets: [{
          label: 'Momentum % (Afstand til MA200)',
          data: {{ chart_values | tojson }},
          backgroundColor: 'rgba(34, 197, 94, 0.6)',
          borderColor: 'rgba(34, 197, 94, 1)',
          borderWidth: 1
        }]
      },
      options: { 
        responsive: true, 
        maintainAspectRatio: false, 
        plugins: { 
          legend: { display: false },
          title: { display: true, text: 'Top 10 Stærkeste Trends (Momentum %)', font: { size: 14 } }
        },
        scales: {
          y: { beginAtZero: true }
        }
      }
    });
  </script>
</body>
</html>
